## packages

Общие пакеты и разделяемый код для всех приложений:

- `shared` — типы, утилиты и общие модели, которые переиспользуются в `apps/*`.

Пакеты подключаются как npm workspaces (monorepo). В прод‑сборке сюда могут попадать только необходимые для рантайма части.

