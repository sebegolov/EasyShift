## deploy

Файлы, связанные с продакшн‑развёртыванием EasyShift.

Сейчас здесь лежит:

- `runtime-package.json` — минимальный `package.json` для установки только прод‑зависимостей на удалённом сервере.

Актуальные команды и структура сервера описаны в `docs/setup/server-deploy.md`.

