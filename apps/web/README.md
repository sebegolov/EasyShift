## apps/web — Веб‑интерфейс

Веб‑клиент EasyShift (в т.ч. для будущего бэк‑офиса и кабинетов через браузер).

Структура:

- `src` — исходники фронтенда.
- `dist` — собранная версия для публикации.

Доступ к тем же API, что и мини‑приложение/бот, чтобы интерфейсы были единообразными.

